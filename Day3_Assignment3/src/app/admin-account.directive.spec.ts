import { AdminAccountDirective } from './admin-account.directive';

describe('AdminAccountDirective', () => {
  it('should create an instance', () => {
    const directive = new AdminAccountDirective();
    expect(directive).toBeTruthy();
  });
});
