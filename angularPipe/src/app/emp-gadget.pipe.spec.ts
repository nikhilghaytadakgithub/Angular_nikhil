import { EmpGadgetPipe } from './emp-gadget.pipe';

describe('EmpGadgetPipe', () => {
  it('create an instance', () => {
    const pipe = new EmpGadgetPipe();
    expect(pipe).toBeTruthy();
  });
});
