import { ShodowDirective } from './shodow.directive';

describe('ShodowDirective', () => {
  it('should create an instance', () => {
    const directive = new ShodowDirective();
    expect(directive).toBeTruthy();
  });
});
