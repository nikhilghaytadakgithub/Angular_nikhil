import { FilterByBtnPipe } from './filter-by-btn.pipe';

describe('FilterByBtnPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByBtnPipe();
    expect(pipe).toBeTruthy();
  });
});
